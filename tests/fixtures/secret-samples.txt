# True positive samples for secret-scan patterns.
# NOTE: high-risk token formats (GitHub PAT, Slack, Anthropic, Stripe) are
# constructed at runtime in tests/hooks/test-secret-patterns.sh via string
# concatenation to avoid GitHub Push Protection. This file keeps only
# officially-documented example values and generic assignments.

# AWS documented example access key (never a real credential)
aws_access_key_id = AKIAIOSFODNN7EXAMPLE

# Generic assignments that MUST trigger
password = "hunter2hunter2"
secret = "not-a-real-secret-value"
api_key = "abcdef1234567890"
